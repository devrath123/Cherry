<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                tools:context="cherry.android.com.cherry.PendingInspectionsActivity">

    <include
        android:id="@+id/pendingInspectionsHeader"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        layout="@layout/app_header"
        />

    <TextView
        android:layout_below="@+id/pendingInspectionsHeader"
        android:id="@+id/pendingInspectionsTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="5dp"
        android:textStyle="bold"
        android:visibility="gone"
        android:text="No Pending Inspections."/>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/pendingInspectionListView"
        android:layout_below="@+id/pendingInspectionsTextView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="2dp"
        android:clickable="true"
        android:scrollbars="vertical"
        />

</RelativeLayout>
